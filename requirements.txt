# Direct dependencies only. Transitive deps are resolved by pip.
# Minimum versions chosen for Python 3.13+ compatibility.

# Data handling
pandas>=2.2
pyarrow>=17.0          # Parquet session storage

# UI
PySide6>=6.10          # 6.10+ supports Python 3.13

# Photo upload (SSH)
paramiko>=3.4
scp>=0.14
requests>=2.31

# Google Sheets (box tracker) - optional, only used by open_boxes_spreadsheet
gspread>=6.0
google-auth>=2.30
google-auth-oauthlib>=1.2

# Packaging into a standalone .exe - optional
pyinstaller>=6.15
